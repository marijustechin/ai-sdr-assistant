/**
 * Public client-safe API of the `sender-profile` entity: read model, types and
 * display helpers. The server-only read API is a separate entry point:
 * `@entities/sender-profile/api`.
 */
export * from "./types";
export * from "./display";
