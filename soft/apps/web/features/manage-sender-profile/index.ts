/**
 * Public API of the `manage-sender-profile` feature: the create/edit form. Its
 * Server Actions, write API and payload builder are internal to the slice.
 */
export { SenderProfileForm } from "./form";
